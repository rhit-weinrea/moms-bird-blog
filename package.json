{
  "name": "moms-bird-blog",
  "version": "0.1.0",
  "description": "Build scripts for Mom's Bird Blog Cloudflare Worker assets",
  "scripts": {
  "dev": "vite",
  "build:assets": "vite build",
  "clean": "rimraf dist",
  "publish:worker": "npm run clean && npm run build:assets && npx wrangler deploy",
  "preview": "vite preview --port 5173"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "rimraf": "^5.0.0"
  },
  "engines": {
    "node": ">=14"
  },
  "license": "MIT"
}
